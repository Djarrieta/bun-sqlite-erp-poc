{
  "name": "bun-sqlite-erp-poc",
  "version": "1.0.0",
  "description": "A modular server-rendered app built with Bun, TypeScript, HTMX and SQLite",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "seeddb": "bun src/scripts/seed.ts",
    "resetdb": "bun src/scripts/reset.ts",
    "fonts": "bun src/scripts/download-fonts.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
